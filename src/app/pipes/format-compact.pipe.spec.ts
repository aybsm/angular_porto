import { FormatCompactPipe } from './format-compact.pipe';

describe('FormatCompactPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCompactPipe();
    expect(pipe).toBeTruthy();
  });
});
